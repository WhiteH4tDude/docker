apt update
apt-get update

apt-get install nano -y
apt-get install git -y
apt-get install wget -y

apt install ubuntu-drivers-common -y
apt install build-essential -y

gcc --version

nvidia-smi

wget https://developer.download.nvidia.com/compute/cuda/12.4.0/local_installers/cuda_12.4.0_550.54.14_linux.run

// Desmarque a opção para instalar o driver da NVIDIA.
sh cuda_12.4.0_550.54.14_linux.run

nano /home/$USER/.bashrc

// Escreva o texto abaixo no terminal
export PATH="/usr/local/cuda-8.0/bin:$PATH" 
export LD_LIBRARY_PATH="/usr/local/cuda-8.0/lib64:$LD_LIBRARY_PATH"

// Ctrl + O > ENTER > Ctrl + X

source /home/$USER/.bashrc

nvcc --version

git clone https://github.com/r4dl/StopThePop --recursive

cd StopThePop

conda env create --file environment_cuda12.yml
conda activate stopthepop

pip install submodules/diff-gaussian-rasterization

// Now you are ready :D 
// https://github.com/r4dl/StopThePop

